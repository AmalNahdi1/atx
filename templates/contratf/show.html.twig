{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Id_conducteur</th>
                <td>{{ contrat.idConducteur }}</td>
            </tr>
            <tr>
                <th>Id_admin</th>
                <td>{{ contrat.idAdmin }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ contrat.dateDebut ? contrat.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ contrat.dateFin ? contrat.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ contrat.prix }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ contrat.statut }}</td>
            </tr>
            <tr>
                <th>QrCode</th>
                <td>{{ contrat.qrCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contratf_index') }}">back to list</a>

    <a href="{{ path('app_contratf_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contratf/_delete_form.html.twig') }}
{% endblock %}
